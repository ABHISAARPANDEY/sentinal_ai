# Primary — matches production Render env and api.js
VITE_API_BASE_URL=http://localhost:8000
# Optional — overrides WebSocket origin; otherwise derived from VITE_API_BASE_URL in src/lib/api.js
VITE_WS_BASE_URL=ws://localhost:8000
# Legacy alias for vite dev proxy only
# VITE_BACKEND_URL=http://localhost:8000
